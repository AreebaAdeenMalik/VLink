# --- 1. IDE & System Metadata (Junk) ---
.idea/
.vscode/
.gitattributes
.gitignore
.dockerignore
*.swp
*.swo

# --- 2. Python Clutter (Junk) ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt

# --- 3. Build Artifacts (Junk) ---
build/
dist/
*.egg-info/
.eggs/

# --- 4. Large Media & Datasets (Space Savers) ---
# Prevent copying gigabytes of test videos or training data
# Add your specific heavy folders here:
datasets/
results/
runs/
*.mp4
*.avi
*.mov
# Note: Keeping .jpg/.png for your test_image.jpg is usually fine,
# but if you have thousands, add *.jpg here too.

# --- 5. CRITICAL: Git History ---
# We deliberately DO NOT ignore .git so you can switch branches inside.
# !.git
```